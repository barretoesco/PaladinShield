# Copia este archivo a .env (no lo subas). Rota cualquier clave que haya aparecido en el repositorio.
# Scripts Node / proceso local:
OPENAI_API_KEY=

# Extension MV3 (demo): clave en `translator.js` como `DEMO_OPENAI_API_KEY` — ver SECURITY_ROADMAP.md
