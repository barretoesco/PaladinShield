# PaladinShield — prompt de politica

El system prompt de auditoria vive en `src/extension/scripts/translator.js` (`AUDITOR_ROLE_PROMPT`). Objetivo: enforcement default-deny, comparacion origen vs payload, y uso forense de `originProfileHint` sin relajar el veredicto.
